<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codelabs.unikomradio">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/iconunikomradio"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconunikomradio"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".mvvm.settings.SettingsActivity"></activity>
        <activity android:name=".mvvm.news.newsdetail.NewsDetailActivity" />
        <activity android:name=".mvvm.programs.programdetail.ProgramDetailActivity" />
        <activity
            android:name=".NoInternet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mvvm.GardenActivity" />
        <activity android:name=".mvvm.main.MainActivity" />
        <activity
            android:name=".mvvm.SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvvm.streaming.streaming_topcharts.StreamingTopchartsActivity" />

        <service android:name=".utilities.services.MediaPlayerServices" />
        <service android:name=".utilities.services.ExoPlayerServices" />

    </application>

</manifest>